// import { API_ROUTES } from '../constants/routes';
// import type { AuthData, UserRegistration } from '../types/api';
// import { baseApiAxios } from './baseApi';

// export const userSignIn = (authData: AuthData) => baseApiAxios.post(API_ROUTES.SIGN_IN, authData);

// export const userRegister = (userData: UserRegistration) => baseApiAxios.post(API_ROUTES.SIGN_UP, userData)